import illustrationDesktop from "./illustrationDesktop.png";
import { StaticImageData } from "next/image";

export const desktopView: { [key: string]: StaticImageData } = {
  illustration: illustrationDesktop,
};
